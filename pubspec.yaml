name: flutter_stickers
description: A new flutter plugin project.
version: 0.0.1

environment:
  sdk: '>=2.1.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  intent: ^1.1.0

flutter:
  plugin:
    androidPackage: dev.chandujs.flutter_stickers
    pluginClass: FlutterStickersPlugin
